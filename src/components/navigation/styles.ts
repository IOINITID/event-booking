import { css } from '@emotion/css';

export const styles = {
  list: css`
    display: grid;
    align-items: center;
    grid-auto-flow: column;
    margin: 0;
    padding: 0;
    list-style: none;
    column-gap: 24px;
  `,
};
